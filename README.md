# Drone Rescue Simulator

Canonical name: drone-rescue-simulator

Current path: /Users/alanchan/projects/drone-simulator

Status: HOLD

## Purpose

This is a browser-based drone rescue training simulator. The current source is a small static web application with an index.html entry point, JavaScript under js/, and supporting assets under assets/.

## Source of truth

- index.html is the application entry point.
- js/ contains the simulator behavior.
- assets/ contains supporting visual and media assets.
- CONTEXT.md records project terminology and domain context.
- package.json records the local start/test commands.

There is no separate generated build directory identified as canonical in this audit.

## Next review

When work resumes, confirm the intended audience, test flow, and deployment method. The proposed canonical name does not authorize a directory rename.
