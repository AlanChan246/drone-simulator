{
  "name": "drone-simulator",
  "private": true,
  "description": "Local static server is required; browsers block GLB loading from file://",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "test": "node --test test/*.test.cjs"
  },
  "dependencies": {
    "blockly": "^9.3.3",
    "three": "^0.128.0"
  }
}
